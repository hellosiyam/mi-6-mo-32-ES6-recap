const students = ['siyam', 'rohul', 'asif', 'abir'];
// students.splice(1);
const student = students.slice(-2);
console.log(student);

const data = {
    name: 'siyam',
    age: 24,
    live: 'bangladesh'
}

delete data.live;
console.log(data);
